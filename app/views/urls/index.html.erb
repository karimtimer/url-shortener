<p style="color: green"><%= notice %></p>

<h1>Url Shortnering Service</h1>

<div id="urls">
  <% @urls.each do |url| %>
    <%= render url %>
    <p>
      <%= link_to "Show this url", url %>
    </p>
  <% end %>
</div>

<%= link_to "New url to shorten", new_url_path %>
